SELECT --> mainly used to select a specigic part of database like column name or raw

 ```sql
 SELECT * FROM users --users is the name of table and it is just example. * means all data from users
 ```
